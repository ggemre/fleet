# My Rust Project

```sh
# Initialize the template:
nix flake init -t github:ggemre/fleet#rust

# Initialize direnv and git
direnv allow
git init

# Build the project
nix build

# Run the project
nix run
```
